services:
  aspen-dev-box:
    volumes:
      - '$ASPEN_DOCKER/error_reporting.ini:/aspen-dev-conf/error_reporting.ini'
      - '$ASPEN_DOCKER/xdebug.ini:/aspen-dev-conf/xdebug.ini'
    environment:
      - WSL_IP
    extra_hosts:
      - host.docker.internal:${WSL_IP:-host-gateway}
